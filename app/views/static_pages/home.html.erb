<% if logged_in? %>
    <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        USER
      </section>
      <section class="micropost_form">
        <%#= render 'shared/micropost_form' %>
      </section>
    </aside>
  </div>
<% elsif nhlogged_in? %>
  <div class="row">
      <section class="user_info">
        <% if @current_nh.parties.any? %>
          <h3>Suas Festas (<%= @current_nh.parties.count %>)</h3>
          <ul class="users">
            <% current_nh.parties.each do |party| %>
              <%= render party %>
            <% end %>
          </ul>
          <%#= will_paginate @parties %>
        <% end %>
      </section>
  </div>
<% else %>
  <h1>BEM-VINDO!</h1>
  <p>Deseja encontrar festas e eventos próximos de você? Faça <%= link_to "Login", login_path %> ou <%= link_to "Cadastre-se", signup_path %>! É dono de uma balada e deseja divulgar suas festas aqui? <%= link_to "Cadastre-se!", signupnh_path %></p>
<% end %>

<div class="row">
  <section class="user_info">
    <h3> Próximos eventos: </h3>
      <ul class="users">
        <% Party.limit(5).each do |party| %>
          <%= render party %>
        <% end %>
      </ul>
      <%#= will_paginate @parties %>
  </section>
</div>
