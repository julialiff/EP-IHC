<%= form_for(@party) do |f| %>
  <% if @party.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
      <% @party.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :add_info %><br>
    <%= f.text_field :add_info %>
  </div>
  <div class="field">
    <%= f.label :music_style %><br>
    <%= f.text_field :music_style %>
  </div>
  <div class="field">
    <%= f.label :NightHouse_id %><br>
    <%= f.number_field :NightHouse_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
